import React from 'react';

import {Header, MovieSearch} from "../../components";

export const SearchPage = () => {

    return (
        <div>
            <Header/>
            <MovieSearch/>
        </div>
    );
};